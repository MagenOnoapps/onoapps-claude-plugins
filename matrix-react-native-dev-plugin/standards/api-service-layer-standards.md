# API Service Layer Standards

_TODO: document this standard. Scope: RTK Query conventions — endpoint organization per feature, tag-based cache invalidation, base query with auth/error interceptor, normalized error shapes._
