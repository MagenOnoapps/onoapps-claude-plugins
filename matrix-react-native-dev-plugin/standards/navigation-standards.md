# Navigation Standards

_TODO: document this standard. Scope: library-agnostic navigation principles (typed routes/params, deep link structure, navigation abstracted behind a service). Navigation library varies per project — defer to the `repo-analyst` agent's detection rather than assuming React Navigation or Expo Router._
